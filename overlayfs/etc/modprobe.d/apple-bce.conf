# apple-bce.conf — Module dependency ordering for T2 Mac hardware
# apple-bce provides keyboard, trackpad, USB camera, and audio transport
# It must load after NVMe to avoid a race condition on T2 hardware
softdep apple-bce pre: nvme
